# Slide outline and capture register

This six-slide draft uses only report-derived figures and text. The deck export and captures stay under ignored `outputs/story-s12/`. There are no patient images, stock photographs or simulated results. The owner should review the copy and source attribution before sharing the deck.

| Slide | Point and on-slide evidence | Source and capture |
|---|---|---|
| 1 | Skin Lesion Lab is an experimental dermoscopy model comparison, not a diagnostic tool. | Title and scope from the accepted S09/S11 reports. No image. |
| 2 | The development comparison uses 1,395 images, 1,037 known groups and three seeds. | `benchmark-s09-recovery-20260921`; notebook `01-groups.ipynb`, cells 1-3 explain the grouping caveat. The local export matches ISIC 2018 Task 3 by counts and fields, without an original download receipt. Attribute the [ISIC challenge collection](https://api.isic-archive.com/collections/66/) and its [CC-BY-NC terms](https://challenge.isic-archive.com/data/). No patient images. |
| 3 | Mean ROC-AUC: 0.891 versus 0.755; paired difference 0.1354 [0.0950, 0.1761]. | Accepted notebook `02-development-report.ipynb`, cell 4, ID `8dd0d8092fe74a7c96281538738b07e2`. Its actual PNG output is preserved as a separate capture with all 27 run IDs. The projected slide enlarges the primary contrast as text because the nine-model figure labels are too small at slide scale. Primary run IDs: `efficientnet_full_unweighted-{17,42,73}` and `logistic-{17,42,73}`. |
| 4 | Calibration and referral do not establish safety. Development primary-run false negatives: 18, 26, 11 of 176. | Accepted notebook `02-development-report.ipynb`, cell 6, ID `8edb47106e1a46a883d545849b8ab81b`. The reliability chart is an actual PNG output, enlarged without cropping. Run IDs are `efficientnet_full_unweighted-{17,42,73}`; the three per-run counts are printed by that cell. |
| 5 | On HIBA, the paired ROC-AUC difference is 0.0481 [-0.0016, 0.1091]; the interval includes zero. | Frozen six-run report `external-s11-hiba-587329`. External results are text, not a fabricated notebook screenshot. HIBA DOI 10.34970/587329, CC-BY, Hospital Italiano de Buenos Aires. |
| 6 | Frozen cutoffs miss many melanoma images; referral changes coverage. This work does not support clinical use or a manuscript. | S11 report and accepted no-go research memo. No image. |

The two figure captures come from the already executed accepted notebook, not a rerun on synthetic data. `outputs/story-s12/build/build.mjs` extracts the PNG payloads from cells 4 and 6 without cropping and stores them as `cell-4-comparison.png` and `cell-6-calibration.png`. Slide 4 embeds the latter. Slide 3 presents the primary contrast in readable text; the full nine-model chart remains an accompanying capture for screen inspection at its native resolution. The layout uses a 1280 by 720 viewport. Inspect every exported slide PNG at full size. Source notebook: `outputs/notebooks/accepted-jzft_hkd/02-development-report.ipynb`. The comparison capture names all 27 development run IDs; the calibration chart names its primary run. Neither figure contains licensed lesion imagery.

Report hashes: development `0f0e898a1af36e05898692a95c9a3f2dfbb59b5c4bb8ad08c144a6e55fd529c7`, external `f592c4c0f8b9491f7db4ad3aedfc9d364a11c8d6fa9f3b57c442ba88e5a645d8`. The accepted development notebook is an S10 execution of that report. Do not swap these figures for a synthetic notebook run without changing the caption and removing measured claims.
